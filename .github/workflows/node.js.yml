name: Node.js CI/CD

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
    build-and-deploy:
        runs-on: self-hosted
        environment: Production
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Use Node.js 20.x
              uses: actions/setup-node@v4
              with:
                  node-version: 20.x
                  cache: "npm"

            - name: Cache npm dependencies
              uses: actions/cache@v3
              with:
                  path: ~/.npm
                  key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
                  restore-keys: |
                      ${{ runner.os }}-node-

            - name: Install dependencies
              run: npm ci

            - name: Create .env file
              run: node ./.github/workflows/create-env.mjs "$SECRETS" "$VARS"
              env:
                  SECRETS: ${{ toJson(secrets) }}
                  VARS: ${{ toJson(vars) }}

            - name: Build
              run: npm run build

            - name: Deploy to production directory
              run: rsync -avz --delete ./ /home/azure/track-the-hack-bot/

            - name: Register slash commands
              working-directory: /home/azure/track-the-hack-bot
              run: npm run register

            - name: Restart the bot with PM2
              working-directory: /home/azure/track-the-hack-bot
              run: pm2 reload track-the-hack-bot --update-env
